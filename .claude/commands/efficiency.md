Analyze **budget efficiency** across all campaigns and adsets.

Execute `./run.sh efficiency` using the budget optimizer script.

**Analyze:**
- Cost per result (CPA) by campaign
- Return on ad spend (ROAS) by campaign
- CPC efficiency relative to benchmarks
- Spend distribution vs performance distribution

Present a ranked table of campaigns by efficiency score.

Flag any campaigns where spend share > performance share (overspending relative to results).

End with actionable recommendations for budget reallocation.
